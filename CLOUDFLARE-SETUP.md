# 🚀 Cloudflare Pages 部署设置指南

## 📋 推荐的部署方式

由于 API Token 权限问题复杂，**推荐使用 Cloudflare Dashboard 的 Git 集成**，这是最简单可靠的方式。

## 🔧 完整设置步骤

### 1. 在 Cloudflare Dashboard 创建 Pages 项目

1. **登录 Cloudflare Dashboard**
   - 访问 [https://dash.cloudflare.com/](https://dash.cloudflare.com/)
   - 登录您的账户

2. **进入 Pages 管理**
   - 点击左侧菜单的 "Workers & Pages"
   - 点击 "Create application"
   - 选择 "Pages" 标签

3. **连接到 Git**
   - 点击 "Connect to Git"
   - 选择 "GitHub"
   - 如果是第一次，需要授权 Cloudflare 访问您的 GitHub 账户
   - 在弹出窗口中授权

4. **选择仓库**
   - 在仓库列表中找到 `Seeed-Solution/sensecraft-hmi-docs`
   - 如果看不到，点击 "Add account or repository"
   - 选择该仓库

5. **配置构建设置**
   ```
   项目名称: sensecraft-hmi-docs
   生产分支: main
   
   构建设置:
   框架预设: Astro
   构建命令: npm run build
   构建输出目录: dist
   根目录: / (留空或默认)
   
   环境变量（可选）:
   NODE_VERSION: 18
   ```

6. **保存并部署**
   - 点击 "Save and Deploy"
   - 等待首次部署完成（约 2-3 分钟）

### 2. 配置自动部署

完成上述步骤后，Cloudflare Pages 会自动：
- ✅ 监听 `main` 分支的推送
- ✅ 自动触发构建和部署
- ✅ 为每个 Pull Request 生成预览链接
- ✅ 提供唯一的 `.pages.dev` 域名

## 🎯 部署流程

### 自动部署流程
```
开发者推送代码到 main 分支
    ↓
Cloudflare Pages 自动检测到更改
    ↓
自动拉取代码并构建
    ↓
自动部署到生产环境
    ↓
网站实时更新
```

### Pull Request 预览
```
创建 Pull Request
    ↓
Cloudflare Pages 自动构建预览
    ↓
在 PR 中显示预览链接
    ↓
审核通过后合并
    ↓
自动部署到生产环境
```

## 🔍 验证部署

1. **检查部署状态**
   - 在 Cloudflare Dashboard 中查看部署历史
   - 查看构建日志

2. **访问网站**
   - 生产环境: `https://sensecraft-hmi-docs.pages.dev/`
   - 或者您配置的自定义域名

3. **测试功能**
   - 检查多语言切换
   - 测试搜索功能
   - 验证所有页面都能正常访问

## 📊 部署配置详情

### 构建设置
```yaml
Framework: Astro
Build command: npm run build
Build output directory: dist
Node.js version: 18
```

### 环境变量（可选）
```
NODE_VERSION=18
PUBLIC_SITE_URL=https://docs.sensecraft-hmi.com
```

### 预览部署
- 每个 PR 都会自动生成预览部署
- 预览 URL 格式: `https://<branch-name>.sensecraft-hmi-docs.pages.dev/`
- PR 合并后，预览部署会自动删除

## 🔄 GitHub Actions 的作用

现在 GitHub Actions 只用于：
- ✅ 验证代码可以正常构建
- ✅ 提供构建状态反馈
- ✅ 上传构建产物供检查

实际部署由 Cloudflare Pages 的 Git 集成自动完成，无需 API Token。

## 🎨 自定义域名（可选）

如果需要使用自定义域名：

1. **在 Cloudflare Pages 项目中**
   - 进入项目设置
   - 点击 "Custom domains"
   - 添加您的域名（如 `docs.sensecraft-hmi.com`）

2. **配置 DNS**
   - Cloudflare 会自动为您配置 DNS 记录
   - 如果域名在 Cloudflare 管理，会自动完成
   - 如果在其他地方，按照指示添加 CNAME 记录

3. **SSL 证书**
   - Cloudflare 会自动提供免费 SSL 证书
   - 证书会自动续期

## 📈 性能优化

Cloudflare Pages 自动提供：
- ✅ 全球 CDN 加速
- ✅ 自动 HTTPS
- ✅ 智能缓存
- ✅ DDoS 防护
- ✅ 资源压缩
- ✅ HTTP/3 支持

## 🚨 故障排除

### 问题1: 构建失败
**解决方法:**
- 检查构建日志
- 确认 Node.js 版本正确
- 本地测试 `npm ci && npm run build`

### 问题2: 页面404
**解决方法:**
- 检查构建输出目录是否正确（应该是 `dist`）
- 确认 `_redirects` 文件在 `dist` 目录中
- 查看部署的文件列表

### 问题3: 自动部署未触发
**解决方法:**
- 检查 Cloudflare Pages 项目的 Git 集成状态
- 确认推送到了正确的分支
- 查看部署历史和日志

## 📞 获取帮助

- **Cloudflare 文档**: [https://developers.cloudflare.com/pages/](https://developers.cloudflare.com/pages/)
- **社区支持**: [Cloudflare Community](https://community.cloudflare.com/)
- **项目问题**: 在 GitHub 创建 Issue

---

**这种方式的优势:**
- ✅ 无需管理 API Token
- ✅ 配置简单，维护方便
- ✅ 完全自动化
- ✅ 内置预览部署
- ✅ 更安全可靠

推荐所有团队成员使用这种方式！
